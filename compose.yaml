services:
  db:
    image: mongo:7.0
    ports:
      - "27017:27017"
    volumes:
      - db-data:/data/db

  app:
    build:
      context: .
    environment:
      MONGODB_URI: mongodb://db:27017/reservationcamps
    ports:
      - "8080:8080"

volumes:
  db-data:
